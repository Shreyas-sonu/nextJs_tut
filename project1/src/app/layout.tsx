export const metadata = {
  title: 'Video Trial',
  description: 'A full stack video streaming App',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
